<<<<<<< HEAD
<Window
    x:Class="ModAssistant.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ModAssistant"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{DynamicResource MainWindow:WindowTitle}"
    Icon="Resources/icon.ico"
    SizeChanged="Window_SizeChanged"
    UIElement.PreviewMouseDown="Window_PreviewMouseDown"
    mc:Ignorable="d"
    MinWidth="1280" MinHeight="720">
=======
<Window x:Class="ModAssistant.OneClickStatus"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModAssistant"
        mc:Ignorable="d"
        Title="OneClick Installer" Height="485" Width="800" WindowStyle="ToolWindow" ResizeMode="NoResize">

    <Window.Resources>
        <local:DivideDoubleByTwoConverter x:Key="DivideDoubleByTwoConverter" />
        <Style x:Key="Spin" TargetType="{x:Type Image}">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="0" CenterX="{Binding Path=ActualWidth, Converter={StaticResource DivideDoubleByTwoConverter}, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Image}}" CenterY="{Binding Path=ActualHeight, Converter={StaticResource DivideDoubleByTwoConverter}, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Image}}" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard x:Name="RotateStarCompass">
                                <DoubleAnimation
                                    AutoReverse="False"
                                    RepeatBehavior="Forever"
                                    Storyboard.TargetProperty="RenderTransform.Angle"
                                    From="0"
                                    To="360"
                                    Duration="0:0:3" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

>>>>>>> bf6286ebd5320dbe1fe271fe21d9bb3887a5829d
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Rectangle Fill="{DynamicResource ModAssistantBackground}" Grid.RowSpan="2"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="400" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
<<<<<<< HEAD
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="65" />
                    <RowDefinition Height="70" />
                    <RowDefinition Height="70" />
                    <RowDefinition Height="70" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Button
                    Name="IntroButton"
                    Grid.Row="0"
                    Height="60"
                    Margin="0,0,10,5"
                    Click="IntroButton_Click"
                    Style="{DynamicResource MainPageButton}">
                    <StackPanel Margin="0,6,0,0">
                        <Image
                            Height="30"
                            VerticalAlignment="Bottom"
                            Source="{StaticResource info_circleDrawingImage}" />
                        <Viewbox Stretch="Uniform" Height="16">
                            <TextBlock
                            HorizontalAlignment="Center"
                            Padding="2,0,2,0"
                            Text="{DynamicResource MainWindow:IntroButton}" FontWeight="SemiBold" />
                        </Viewbox>
                    </StackPanel>
                </Button>

                <Button
                    Name="ModsButton"
                    Grid.Row="1"
                    Height="60"
                    Margin="0,5,10,5"
                    Click="ModsButton_Click"
                    IsEnabled="false"
                    Style="{DynamicResource MainPageButton}">
                    <StackPanel Margin="0,6,0,0">
                        <Image
                            Height="30"
                            VerticalAlignment="Bottom"
                            Source="{StaticResource microchipDrawingImage}" />
                        <Viewbox Stretch="Uniform" Height="16">
                            <TextBlock
                            HorizontalAlignment="Center"
                            Padding="2,0,2,0"
                            Text="{DynamicResource MainWindow:ModsButton}" FontWeight="SemiBold" />
                        </Viewbox>
                    </StackPanel>
                </Button>

                <Button
                    Name="AboutButton"
                    Grid.Row="3"
                    Height="60"
                    Margin="0,10,10,0"
                    Click="AboutButton_Click"
                    Style="{DynamicResource MainPageButton}">
                    <StackPanel Margin="0,6,0,0">
                        <Image
                            Height="30"
                            VerticalAlignment="Bottom"
                            Source="{StaticResource heartDrawingImage}" />
                        <Viewbox Stretch="Uniform" Height="16">
                            <TextBlock
                            HorizontalAlignment="Center"
                            Padding="2,0,2,0"
                            Text="{DynamicResource MainWindow:AboutButton}" FontWeight="SemiBold" />
                        </Viewbox>
                    </StackPanel>
                </Button>

                <Button
                    Name="OptionsButton"
                    Grid.Row="2"
                    Height="60"
                    Margin="0,5,10,5"
                    Click="OptionsButton_Click"
                    Style="{DynamicResource MainPageButton}">
                    <StackPanel Margin="0,6,0,0">
                        <Image
                            Height="30"
                            VerticalAlignment="Bottom"
                            Source="{StaticResource cogDrawingImage}" />
                        <Viewbox Stretch="Uniform" Height="16">
                            <TextBlock
                            HorizontalAlignment="Center"
                            Padding="2,0,2,0"
                            Text="{DynamicResource MainWindow:OptionsButton}" FontWeight="SemiBold" />
                        </Viewbox>
                    </StackPanel>
                </Button>
                <Button
                    Name="PlayButton"
                    Grid.Row="4"
                    Margin="0,0,10,0"
                    Click="PlayButton_Click"
                    Style="{DynamicResource MainPageButton}"
                    Height="60"
                    VerticalAlignment="Bottom">
                    <StackPanel Margin="0,0,0,0">
                        <Image
                            Height="30"
                            VerticalAlignment="Bottom"
                            Source="{StaticResource playDrawingImage}" />
                        <Viewbox Stretch="Uniform" Height="16">
                            <TextBlock
                            HorizontalAlignment="Center"
                            Padding="2,0,2,0"
                            Text="{DynamicResource MainWindow:PlayButton}" />
                        </Viewbox>
                    </StackPanel>
                </Button>

                <StackPanel
                    Name="GameVersions"
                    Grid.Row="5"
                    VerticalAlignment="Bottom">
                    <TextBlock FontSize="10">
                        <TextBlock Text="{DynamicResource MainWindow:GameVersionLabel}" RenderTransformOrigin="0.94,0.41" Width="65" TextAlignment="Center" />
                    </TextBlock>
                    <ComboBox
                        Name="GameVersionsBox"
                        Margin="0,5,5,0"
                        SelectionChanged="GameVersionsBox_SelectionChanged" />
                    <ComboBox
                        Name="GameVersionsBoxOverride"
                        Margin="0,5,5,0"
                        Visibility="Collapsed" />
                </StackPanel>
=======
>>>>>>> bf6286ebd5320dbe1fe271fe21d9bb3887a5829d

            <Image
                Grid.Row="0"
                Margin="1,0"
                HorizontalAlignment="Left"
                Source="{DynamicResource loadingInnerDrawingImage}"
                Stretch="Uniform" />
            <Image
                Grid.Row="0"
                Margin="1,0"
                HorizontalAlignment="Left"
                Source="{DynamicResource loadingMiddleDrawingImage}"
                Stretch="Uniform" />
            <Image
                Name="Ring"
                Grid.Row="0"
                Margin="1,0"
                HorizontalAlignment="Left"
                Source="{DynamicResource loadingOuterDrawingImage}"
                Style="{StaticResource Spin}" />
            <ScrollViewer
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Auto"
                    Background="{DynamicResource BottomStatusBarBackground}"
                    Margin="401,10,10,10">
                <TextBox
                        x:Name="HistoryTextBlock"
                        Padding="5"
                        Background="{DynamicResource BottomStatusBarBackground}"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextColor}" />
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Border
                Grid.Row="0"
                Height="40"
                                Margin="10,0,10,10"

<<<<<<< HEAD
                <Border
                    Height="40"
                    VerticalAlignment="Bottom"
                    BorderBrush="{DynamicResource BottomStatusBarOutline}"
                    CornerRadius="2"
                    BorderThickness="1">
                    <TextBlock
                        Name="MainTextBlock"
                        Padding="5"
                        Background="{DynamicResource BottomStatusBarBackground}"
                        FontSize="20" >
                    </TextBlock>
                </Border>
                <Button
                    Name="InfoButton"
                    Grid.Column="1"
                    Height="40"
                    MinWidth="115"
                    Margin="10,10,0,0"
                    Padding="20,0,20,0"
                    HorizontalAlignment="Right"
                    Click="InfoButton_Click"
                    IsEnabled="False">
                    <StackPanel>
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Text="{DynamicResource MainWindow:ModInfoButton}" />
                    </StackPanel>
                </Button>
                <Button
                    Name="InstallButton"
                    Grid.Column="2"
                    Height="40"
                    MinWidth="115"
                    Margin="10,10,0,0"
                    Padding="20,0,20,0"
                    HorizontalAlignment="Right"
                    Click="InstallButton_Click"
                    IsEnabled="False">
                    <StackPanel>
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Text="{DynamicResource MainWindow:InstallButtonTop}" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Text="{DynamicResource MainWindow:InstallButtonBottom}" />
                    </StackPanel>
                </Button>
            </Grid>
=======
                VerticalAlignment="Bottom"
                BorderBrush="{DynamicResource BottomStatusBarOutline}"
                BorderThickness="1">
                <TextBlock
                        Name="MainTextBlock"
                        Padding="5"
                        Background="{DynamicResource BottomStatusBarBackground}"
                        FontSize="20" />
            </Border>
>>>>>>> bf6286ebd5320dbe1fe271fe21d9bb3887a5829d
        </Grid>
    </Grid>
</Window>
